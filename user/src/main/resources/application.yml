server:
  port: 8002

spring:
  application:
    name: user-service
  profiles:
    active: dev

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.entity # 所有entity别名类所在的包

